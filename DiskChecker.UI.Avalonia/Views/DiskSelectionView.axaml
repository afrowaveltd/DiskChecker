<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:DiskChecker.UI.Avalonia.ViewModels"
             xmlns:converters="using:DiskChecker.UI.Avalonia.Converters"
             x:Class="DiskChecker.UI.Avalonia.Views.DiskSelectionView"
             Background="{DynamicResource ThemeBackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ========== HEADER ========== -->
        <Border Grid.Row="0" Background="{DynamicResource ThemePanelBrush}" BorderBrush="{DynamicResource ThemeBorderBrush}" BorderThickness="0,0,0,1" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,0,5">
                    <TextBlock Text="💾 Vyberte Disk pro Testování" 
                               FontSize="28" FontWeight="Bold" Foreground="#1a1a1a" Margin="0,0,0,5"/>
                    <TextBlock Text="Zvolte disk, který chcete diagnostikovat" 
                               FontSize="13" Foreground="#444444"/>
                    <TextBlock Text="{Binding LoadingState}" FontSize="11" Foreground="#333333" Margin="0,4,0,0"/>
                    <ProgressBar IsIndeterminate="True" Margin="0,6,200,0" Background="#E6EAF0" Foreground="#004B93">
                        <ProgressBar.IsVisible>
                            <Binding Path="IsBusy" />
                        </ProgressBar.IsVisible>
                    </ProgressBar>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="🔄 Reload disků"
                        Command="{Binding ReloadDisksCommand}"
                        Background="#004B93"
                        Foreground="White"
                        VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- ========== DASHBOARD ========== -->
        <Grid Grid.Row="1" Margin="20,12,20,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{DynamicResource ThemePanelBrush}" BorderBrush="{DynamicResource ThemeBorderBrush}" BorderThickness="1" Padding="14" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="📊 Rychlý SMART report systémového disku" FontSize="13" FontWeight="Bold" Foreground="#333333" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding SystemDiskName}" FontSize="12" FontWeight="SemiBold" Foreground="#1A1A1A" Margin="0,0,0,6"/>
                    <TextBlock FontSize="12" Foreground="#007ACC" Margin="0,0,0,4">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="Známka: {0}">
                                <Binding Path="SystemDiskGrade"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                    <TextBlock FontSize="12" Foreground="#333333" Margin="0,0,0,4">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="Teplota: {0}">
                                <Binding Path="SystemDiskTemperature"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                    <TextBlock Text="{Binding SystemDiskSummary}" FontSize="11" Foreground="#444444" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="{DynamicResource ThemePanelBrush}" BorderBrush="{DynamicResource ThemeBorderBrush}" BorderThickness="1" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="📋 Poslední testy" FontSize="13" FontWeight="Bold" Foreground="#333333" Margin="0,0,0,8"/>

                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding RecentTests}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Datum" Binding="{Binding TestDate}" Width="130"/>
                            <DataGridTextColumn Header="Disk" Binding="{Binding DriveName}" Width="*"/>
                            <DataGridTextColumn Header="Typ" Binding="{Binding TestType}" Width="120"/>
                            <DataGridTextColumn Header="Známka" Binding="{Binding Grade}" Width="80"/>
                            <DataGridTextColumn Header="Skóre" Binding="{Binding Score}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <!-- ========== DISK LIST ========== -->
        <Border Grid.Row="2" Background="{DynamicResource ThemePanelBrush}" BorderBrush="{DynamicResource ThemeBorderBrush}" BorderThickness="1" Margin="20,0,20,20" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock Text="💿 Vyberte disk z nabídky" FontSize="14" FontWeight="Bold" Foreground="#333333"/>
                    <TextBlock Text="  |  🔒 = Zamčeno (destruktivní operace blokovány)" FontSize="11" Foreground="#E74C3C" VerticalAlignment="Center"/>
                </StackPanel>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding DiskCards}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="3" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="10" Cursor="Hand">
                                    <Border Name="DiskCard" 
                                            Background="#FFFFFF" 
                                            BorderBrush="#D0D0D0" 
                                            BorderThickness="1"
                                            CornerRadius="6"
                                            Padding="16"
                                            MinHeight="200">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>

                                            <!-- Icon and Lock Symbol -->
                                            <Grid Grid.Row="0" Margin="0,0,0,8">
                                                <TextBlock Text="💾" FontSize="32" HorizontalAlignment="Left"/>
                                                <!-- Lock icon overlay for locked disks -->
                                                <TextBlock Text="🔒" FontSize="20" 
                                                          HorizontalAlignment="Right" VerticalAlignment="Top"
                                                          Foreground="#E74C3C">
                                                    <TextBlock.IsVisible>
                                                        <Binding Path="IsLocked"/>
                                                    </TextBlock.IsVisible>
                                                </TextBlock>
                                            </Grid>

                                            <!-- Name & Path -->
                                            <StackPanel Grid.Row="1" Margin="0,0,0,8">
                                                <TextBlock Text="{Binding DisplayName}" 
                                                          FontSize="14" FontWeight="Bold" Foreground="#1a1a1a" Margin="0,0,0,2"/>
                                                <TextBlock Text="{Binding DisplayPath}" 
                                                          FontSize="10" Foreground="#555555" TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>

                                            <!-- Key Metrics -->
                                            <StackPanel Grid.Row="2" Margin="0,0,0,8">
                                                <Grid Margin="0,0,0,4">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="Velikost: " FontSize="10" Foreground="#444444"/>
                                                    <TextBlock Grid.Column="1" Text="{Binding CapacityText}" FontSize="10" FontWeight="Bold" Foreground="#004B93"/>
                                                </Grid>
                                                
                                                <Grid Margin="0,0,0,4">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="SMART: " FontSize="10" Foreground="#444444"/>
                                                    <TextBlock Grid.Column="1" Text="{Binding GradeText}" FontSize="10" FontWeight="Bold" Foreground="#004B93"/>
                                                </Grid>

                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="Teplota: " FontSize="10" Foreground="#444444"/>
                                                    <TextBlock Grid.Column="1" Text="{Binding TemperatureText}" FontSize="10" FontWeight="Bold" Foreground="#D32F2F"/>
                                                </Grid>

                                                <TextBlock Text="{Binding IsSystemDiskLabel}" 
                                                          FontSize="9" Foreground="#FF9800" Margin="0,6,0,0" FontWeight="Bold">
                                                    <TextBlock.IsVisible>
                                                        <Binding Path="IsSystemDisk"/>
                                                    </TextBlock.IsVisible>
                                                </TextBlock>
                                                
                                                <TextBlock Text="{Binding LockStatusText}" 
                                                          FontSize="9" Foreground="#E74C3C" Margin="0,2,0,0" FontWeight="SemiBold" TextWrapping="Wrap">
                                                    <TextBlock.IsVisible>
                                                        <Binding Path="IsLocked"/>
                                                    </TextBlock.IsVisible>
                                                </TextBlock>
                                            </StackPanel>

                                            <!-- Selection Button (transparent overlay) - MUST BE FIRST for proper z-order -->
                                            <Button Grid.Row="0" Grid.RowSpan="4"
                                                    Command="{Binding $parent[ItemsControl].DataContext.SelectDiskCommand}"
                                                    CommandParameter="{Binding}"
                                                    Background="Transparent"
                                                    HorizontalAlignment="Stretch"
                                                    VerticalAlignment="Stretch"
                                                    Margin="0,0,0,40">
                                                <Button.Template>
                                                    <ControlTemplate>
                                                        <Border Background="Transparent" Cursor="Hand"/>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>

                                            <!-- Lock/Unlock Button (only for non-system disks) - MUST BE LAST to be clickable -->
                                            <Button Grid.Row="3"
                                                    Command="{Binding $parent[ItemsControl].DataContext.ToggleLockDiskCommand}"
                                                    CommandParameter="{Binding}"
                                                    Background="#607D8B"
                                                    Foreground="White"
                                                    FontSize="10"
                                                    Padding="8,4"
                                                    HorizontalAlignment="Center"
                                                    IsVisible="{Binding IsSystemDisk, Converter={StaticResource InverseBooleanConverter}}"
                                                    ZIndex="999">
                                                <TextBlock Text="{Binding IsLocked, Converter={StaticResource LockTextConverter}}"/>
                                            </Button>
                                        </Grid>
                                    </Border>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- ========== STATUS BAR ========== -->
        <TextBlock Grid.Row="4" Text="{Binding StatusMessage}" 
                   Background="#F0F0F0" Padding="20,8" 
                   FontSize="11" Foreground="#333333"
                   TextTrimming="CharacterEllipsis"/>
    </Grid>
</UserControl>