<UserControl x:Class="DiskChecker.UI.WPF.Views.AnalysisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:oxy="http://oxyplot.org/wpf"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="1000"
             Background="#F5F5F5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="📈 Analýza disku v čase" FontSize="24" FontWeight="Bold" Foreground="#1a1a1a"/>

                <ComboBox Grid.Column="1"
                          Width="340"
                          ItemsSource="{Binding Drives}"
                          DisplayMemberPath="DriveName"
                          SelectedItem="{Binding SelectedDrive}"
                          Margin="0,0,10,0"/>

                <Button Grid.Column="2"
                        Content="🔄 Obnovit"
                        Command="{Binding RefreshAnalysisCommand}"
                        Style="{StaticResource ActionButtonStyle}"
                        Background="#004B93"
                        Foreground="White"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="20,15,20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="1" Padding="8" Margin="0,0,10,0">
                <oxy:PlotView Model="{Binding TrendPlotModel}"/>
            </Border>

            <Border Grid.Column="1" Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="1" Padding="10">
                <DataGrid ItemsSource="{Binding DriveHistory}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          RowHeaderWidth="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Datum" Binding="{Binding TestDate, StringFormat={}{0:dd.MM.yyyy HH:mm}}" Width="140"/>
                        <DataGridTextColumn Header="Typ" Binding="{Binding TestType}" Width="*"/>
                        <DataGridTextColumn Header="Známka" Binding="{Binding Grade}" Width="80"/>
                        <DataGridTextColumn Header="Skóre" Binding="{Binding Score, StringFormat={}{0:F1}}" Width="80"/>
                        <DataGridTextColumn Header="Chyby" Binding="{Binding ErrorCount}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2"
                   Text="{Binding StatusMessage}"
                   Background="#F0F0F0"
                   Padding="20,8"
                   FontSize="11"
                   Foreground="#666666"
                   TextTrimming="CharacterEllipsis"/>
    </Grid>
</UserControl>
