<UserControl x:Class="DiskChecker.UI.WPF.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#F5F5F5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="20">
            <TextBlock Text="⚙️ Správa databáze a archivace" FontSize="24" FontWeight="Bold" Foreground="#1a1a1a"/>
        </Border>

        <Border Grid.Row="1" Background="#FFFFFF" Margin="20" BorderBrush="#E0E0E0" BorderThickness="1" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                    <Button Content="🔄 Obnovit přehled" Command="{Binding RefreshDatabaseOverviewCommand}" Style="{StaticResource ActionButtonStyle}" Background="#004B93" Foreground="White" Margin="0,0,10,0"/>
                    <Button Content="🧹 Údržba databáze" Command="{Binding CleanupDatabaseCommand}" Style="{StaticResource ActionButtonStyle}" Background="#B35A00" Foreground="White" Margin="0,0,10,0"/>
                    <Button Content="📦 Archivovat" Command="{Binding ArchiveSelectedDriveCommand}" Style="{StaticResource ActionButtonStyle}" Background="#005A2B" Foreground="White" Margin="0,0,10,0"/>
                    <Button Content="📋 Pohřební list" Command="{Binding GenerateDecommissionReportCommand}" Style="{StaticResource ActionButtonStyle}" Background="#DC3545" Foreground="White"/>
                    <CheckBox Content="Dry-run (bez mazání)" IsChecked="{Binding IsDryRun}" Margin="14,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#5F6B7A"/>
                </StackPanel>

                <Border Grid.Row="1" Background="#FFF3CD" BorderBrush="#FFC107" BorderThickness="1" Padding="10" Margin="0,0,0,10" CornerRadius="4">
                    <TextBlock Text="⚠️ Vyberte disk v tabulce pro archivaci nebo pohřební list. Pohřební list vytvoří dokument o životnosti disku při jeho vyřazení." 
                               Foreground="#856404" FontSize="11" TextWrapping="Wrap"/>
                </Border>

                <TextBlock Grid.Row="2" Text="{Binding TotalHistoryItems, StringFormat=Aktivní testy v DB: {0}}" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,8"/>

                <DataGrid Grid.Row="3"
                          ItemsSource="{Binding Drives}"
                          SelectedItem="{Binding SelectedDrive}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          RowHeaderWidth="0"
                          HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Disk" Binding="{Binding DriveName}" Width="*"/>
                        <DataGridTextColumn Header="Sériové číslo" Binding="{Binding SerialNumber}" Width="220"/>
                        <DataGridTextColumn Header="Testů" Binding="{Binding TotalTests}" Width="90"/>
                        <DataGridTextColumn Header="Poslední test" Binding="{Binding LastTestDate, StringFormat={}{0:dd.MM.yyyy HH:mm}}" Width="160"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,12,0,0">
                    <TextBox Width="350" Text="{Binding ArchivePath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <Button Content="📥 Importovat ZIP" Command="{Binding ImportArchiveCommand}" Style="{StaticResource ActionButtonStyle}" Background="#5F6B7A" Foreground="White"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
