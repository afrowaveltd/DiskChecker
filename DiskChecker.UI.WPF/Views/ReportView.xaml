<UserControl x:Class="DiskChecker.UI.WPF.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#F5F5F5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="20">
            <TextBlock Text="📄 Reporty Testů" FontSize="24" FontWeight="Bold" Foreground="#1a1a1a"/>
        </Border>

        <Border Grid.Row="1" Background="#FFFFFF" Margin="20,15,20,10" BorderBrush="#E0E0E0" BorderThickness="1" Padding="12">
            <Button Content="🔄 Obnovit report"
                    Command="{Binding RefreshReportCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InvertBoolConverter}}"
                    Style="{StaticResource ActionButtonStyle}"
                    Background="#004B93"
                    Foreground="White"
                    Padding="12,8"
                    HorizontalAlignment="Left"
                    FontWeight="Bold"
                    Cursor="Hand"/>
        </Border>

        <Grid Grid.Row="2" Margin="20,0,20,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <UniformGrid Grid.Row="0" Columns="4" Margin="0,0,0,10">
                <Border Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="1" Padding="12" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="Celkem testů" Foreground="#666666" FontSize="11"/>
                        <TextBlock Text="{Binding TotalTests}" FontSize="20" FontWeight="Bold" Foreground="#1a1a1a"/>
                    </StackPanel>
                </Border>
                <Border Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="1" Padding="12" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="SMART testy" Foreground="#666666" FontSize="11"/>
                        <TextBlock Text="{Binding SmartCheckTests}" FontSize="20" FontWeight="Bold" Foreground="#004B93"/>
                    </StackPanel>
                </Border>
                <Border Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="1" Padding="12" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="Surface testy" Foreground="#666666" FontSize="11"/>
                        <TextBlock Text="{Binding SurfaceTests}" FontSize="20" FontWeight="Bold" Foreground="#005A2B"/>
                    </StackPanel>
                </Border>
                <Border Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="1" Padding="12">
                    <StackPanel>
                        <TextBlock Text="Průměrné skóre" Foreground="#666666" FontSize="11"/>
                        <TextBlock Text="{Binding AverageScore, StringFormat={}{0:F1}}" FontSize="20" FontWeight="Bold" Foreground="#1a1a1a"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Border Grid.Row="1" Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="1" Padding="10">
                <DataGrid ItemsSource="{Binding RecentTests}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          RowHeaderWidth="0"
                          Background="White"
                          BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Datum" Binding="{Binding TestDate, StringFormat={}{0:dd.MM.yyyy HH:mm}}" Width="180"/>
                        <DataGridTextColumn Header="Disk" Binding="{Binding DriveName}" Width="*"/>
                        <DataGridTextColumn Header="Typ" Binding="{Binding TestType}" Width="150"/>
                        <DataGridTextColumn Header="Známka" Binding="{Binding Grade}" Width="90"/>
                        <DataGridTextColumn Header="Skóre" Binding="{Binding Score, StringFormat={}{0:F1}}" Width="90"/>
                        <DataGridTextColumn Header="Chyby" Binding="{Binding ErrorCount}" Width="90"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <TextBlock Grid.Row="3"
                   Text="{Binding StatusMessage}"
                   Background="#F0F0F0"
                   Padding="20,8"
                   FontSize="11"
                   Foreground="#666666"
                   TextTrimming="CharacterEllipsis"/>
    </Grid>
</UserControl>
