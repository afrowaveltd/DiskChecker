<UserControl x:Class="DiskChecker.UI.WPF.Views.SmartCheckView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#F5F5F5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="20">
            <TextBlock Text="🔧 SMART Kontrola Disku" FontSize="24" FontWeight="Bold" Foreground="#1a1a1a"/>
        </Border>

        <Border Grid.Row="1" Background="#FFFFFF" Margin="20" BorderBrush="#E0E0E0" BorderThickness="1" Padding="20">
            <TextBlock Text="{Binding StatusMessage}" FontSize="14" Foreground="#666666" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
